# Copia questo file in .env.local e inserisci i valori del tuo progetto Supabase
# (Dashboard → Project Settings → API). Senza questi valori l'app gira in
# modalità locale (dati nel browser, login simulato).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
