# Avatar 3D e capi 3D — Piano di implementazione

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sostituire il collage 2D difettoso con due modalità a scelta dell'utente — un avatar 3D ruotabile e un avatar piatto corretto — entrambe alimentate da una pipeline che scontorna la foto del capo.

**Architecture:** La logica pura (scontorno, colore dominante, profili di geometria) vive in `src/utils/` ed è testata in Vitest su ambiente `node`. I componenti React (`Avatar3D`, `AvatarSvg`) la consumano. `Avatar3D` è l'unico file che parla con three.js, caricato con `React.lazy` dalla sola `TryOnPage`. `OutfitOnAvatar` ospita il selettore 3D/Piatto.

**Tech Stack:** React 19, Vite 8, Vitest 4 (`environment: 'node'`), three.js (nuova dipendenza), i18next.

## Global Constraints

- **I test girano in ambiente `node`** (`vite.config.js`): nessun DOM, nessun canvas, nessun WebGL. Ogni funzione testata deve essere pura e operare su strutture dati semplici (`{ data: Uint8ClampedArray, width, height }`), mai su `HTMLCanvasElement`.
- **Vitest raccoglie solo `src/**/*.test.js`** — i file di test stanno accanto al modulo, come `tryonComposer.test.js`.
- **I 58 test esistenti devono restare verdi** a ogni commit.
- **Commit in italiano** (convenzione del repo), firmati `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **`src/utils/tryonComposer.js` non si tocca.** I `kind` che produce (`top`, `dress`, `bottom`, `outerwear`, `shoes`) sono il contratto fra composizione e resa.
- **`getBodyWidthFactor(bodyShapeId)`** da `src/utils/avatarOptions.js` è l'unica fonte della corporatura. Valori: slim 0.88, average 1, athletic 1.08, curvy 1.16, plus 1.28.
- **three.js entra solo nel chunk di `/tryon`**: importarlo unicamente dentro `Avatar3D.jsx`, che va caricato con `React.lazy`.
- **Nessun testo promette gratuità dove non c'è.** Le tre modalità dichiarano costo e limiti (tabella in §3 della spec).
- Spec di riferimento: `docs/superpowers/specs/2026-07-09-avatar-3d-design.md`.

---

## Struttura dei file

| File | Responsabilità |
|---|---|
| `src/utils/garmentTexture.js` (nuovo) | **Puro.** Maschera dello sfondo, bounding box del capo, colore dominante, regole di degradazione. Zero DOM. |
| `src/utils/garmentTexture.test.js` (nuovo) | Test del sopra, su immagini sintetiche costruite a mano. |
| `src/utils/garmentImage.js` (nuovo) | **Guscio browser.** Carica la foto, la porta in canvas, chiama `garmentTexture`, restituisce un PNG ritagliato e trasparente. Gestisce CORS. Non testato in unit (tocca il DOM), verificato a schermo. |
| `src/utils/avatarMesh.js` (nuovo) | **Puro.** Profili di rotazione del corpo dai parametri di `avatar_config`. |
| `src/utils/avatarMesh.test.js` (nuovo) | Test del sopra. |
| `src/utils/garmentMesh.js` (nuovo) | **Puro.** Profili di rotazione dei cinque capi, come guscio del corpo. |
| `src/utils/garmentMesh.test.js` (nuovo) | Test del sopra. |
| `src/utils/webgl.js` (nuovo) | Rilevamento del supporto WebGL. |
| `src/components/Avatar/Avatar3D.jsx` (nuovo) | Scena three.js, rotazione al trascinamento, rendering su richiesta. |
| `src/components/Avatar/AvatarSvg.jsx` (modifica) | Modalità piatta: disegna il capo scontornato intero, niente `clipPath`. |
| `src/components/Avatar/OutfitOnAvatar.jsx` (modifica) | Selettore 3D/Piatto, caricamento texture, `React.lazy` di `Avatar3D`. |
| `src/components/Avatar/Avatar.css` (modifica) | Stili del selettore e del contenitore 3D. |
| `src/pages/TryOnPage/TryOnPage.jsx` (modifica) | Riga informativa con costo e limiti su ogni scheda. |
| `src/i18n/it.json`, `src/i18n/en.json` (modifica) | Testi del selettore e delle righe di costo. |

---

## Task 1: `garmentTexture.js` — maschera dello sfondo

**Files:**
- Create: `src/utils/garmentTexture.js`
- Test: `src/utils/garmentTexture.test.js`

**Interfaces:**
- Consumes: niente.
- Produces: `backgroundMask(image, opts) -> Uint8Array` dove `image` è `{ data: Uint8ClampedArray, width: number, height: number }` in RGBA e il risultato ha `width*height` byte, `1` = pixel del capo, `0` = sfondo. `opts` è `{ tolerance?: number }`, default `BG_TOLERANCE = 32`. Esporta anche `BG_TOLERANCE`.

**Perché così:** lo sfondo di una foto di capo è quasi sempre uniforme e tocca i bordi. Un riempimento a partire dai quattro angoli, che si propaga finché il colore resta vicino a quello dell'angolo di partenza, lo isola senza modelli né reti neurali. È deterministico, quindi testabile.

- [ ] **Step 1: Scrivere il test che fallisce**

Crea `src/utils/garmentTexture.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { backgroundMask, BG_TOLERANCE } from './garmentTexture';

/** Costruisce un'immagine RGBA piena di `bg`, con un rettangolo di `fg`. */
const makeImage = (width, height, bg, fg, rect) => {
  const data = new Uint8ClampedArray(width * height * 4);
  for (let y = 0; y < height; y++) {
    for (let x = 0; x < width; x++) {
      const inRect =
        rect &&
        x >= rect.x &&
        x < rect.x + rect.width &&
        y >= rect.y &&
        y < rect.y + rect.height;
      const [r, g, b] = inRect ? fg : bg;
      const i = (y * width + x) * 4;
      data[i] = r;
      data[i + 1] = g;
      data[i + 2] = b;
      data[i + 3] = 255;
    }
  }
  return { data, width, height };
};

const WHITE = [255, 255, 255];
const BLUE = [40, 79, 127];

describe('backgroundMask', () => {
  it('marca come capo solo il rettangolo centrale su sfondo uniforme', () => {
    const img = makeImage(10, 10, WHITE, BLUE, { x: 3, y: 3, width: 4, height: 4 });
    const mask = backgroundMask(img);

    expect(mask).toHaveLength(100);
    expect(mask[0]).toBe(0); // angolo = sfondo
    expect(mask[4 * 10 + 4]).toBe(1); // centro = capo
    expect(mask.reduce((a, b) => a + b, 0)).toBe(16); // 4x4
  });

  it('tollera un lieve rumore sullo sfondo entro la soglia', () => {
    const img = makeImage(6, 6, [250, 250, 250], BLUE, { x: 2, y: 2, width: 2, height: 2 });
    // un pixel di sfondo leggermente diverso, ma dentro la tolleranza
    img.data[(0 * 6 + 3) * 4] = 250 - (BG_TOLERANCE - 5);
    const mask = backgroundMask(img);
    expect(mask[0 * 6 + 3]).toBe(0);
    expect(mask.reduce((a, b) => a + b, 0)).toBe(4);
  });

  it("non considera sfondo un'area del colore dello sfondo ma isolata dai bordi", () => {
    // Una "tasca" bianca dentro il capo resta parte del capo: non tocca i bordi.
    const img = makeImage(9, 9, WHITE, BLUE, { x: 2, y: 2, width: 5, height: 5 });
    const i = (4 * 9 + 4) * 4;
    img.data[i] = 255;
    img.data[i + 1] = 255;
    img.data[i + 2] = 255;
    const mask = backgroundMask(img);
    expect(mask[4 * 9 + 4]).toBe(1);
  });

  it('restituisce tutto sfondo se l’immagine è di un solo colore', () => {
    const img = makeImage(5, 5, WHITE, WHITE, null);
    const mask = backgroundMask(img);
    expect(mask.reduce((a, b) => a + b, 0)).toBe(0);
  });
});
```

- [ ] **Step 2: Eseguire il test e verificare che fallisca**

Run: `npx vitest run src/utils/garmentTexture.test.js`
Expected: FAIL — `Failed to resolve import "./garmentTexture"`.

- [ ] **Step 3: Scrivere l'implementazione minima**

Crea `src/utils/garmentTexture.js`:

```js
/**
 * Estrazione del capo dalla sua foto: sfondo via, rettangolo del capo, colore
 * dominante. Tutto puro e senza DOM, così è testabile in Vitest (ambiente node).
 * Il guscio che tocca il canvas sta in garmentImage.js.
 */

/** Distanza di Chebyshev fra due colori RGB: massimo scarto su un canale. */
const colorDistance = (data, i, r, g, b) =>
  Math.max(
    Math.abs(data[i] - r),
    Math.abs(data[i + 1] - g),
    Math.abs(data[i + 2] - b)
  );

export const BG_TOLERANCE = 32;

/**
 * Riempimento dai quattro angoli: un pixel è sfondo se è raggiungibile da un
 * angolo attraverso pixel cromaticamente vicini a quell'angolo. Una zona del
 * colore dello sfondo ma circondata dal capo resta capo, perché non è raggiungibile.
 *
 * @returns {Uint8Array} 1 = capo, 0 = sfondo
 */
export function backgroundMask({ data, width, height }, { tolerance = BG_TOLERANCE } = {}) {
  const isBackground = new Uint8Array(width * height);
  const queue = [];

  const corners = [
    [0, 0],
    [width - 1, 0],
    [0, height - 1],
    [width - 1, height - 1],
  ];

  for (const [cx, cy] of corners) {
    const p = cy * width + cx;
    if (isBackground[p]) continue;
    const i = p * 4;
    const [r, g, b] = [data[i], data[i + 1], data[i + 2]];
    isBackground[p] = 1;
    queue.push(p);

    while (queue.length) {
      const cur = queue.pop();
      const x = cur % width;
      const y = (cur - x) / width;
      const neighbours = [
        x > 0 ? cur - 1 : -1,
        x < width - 1 ? cur + 1 : -1,
        y > 0 ? cur - width : -1,
        y < height - 1 ? cur + width : -1,
      ];
      for (const n of neighbours) {
        if (n < 0 || isBackground[n]) continue;
        if (colorDistance(data, n * 4, r, g, b) <= tolerance) {
          isBackground[n] = 1;
          queue.push(n);
        }
      }
    }
  }

  const mask = new Uint8Array(width * height);
  for (let p = 0; p < mask.length; p++) mask[p] = isBackground[p] ? 0 : 1;
  return mask;
}
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `npx vitest run src/utils/garmentTexture.test.js`
Expected: PASS, 4 test.

- [ ] **Step 5: Commit**

```bash
git add src/utils/garmentTexture.js src/utils/garmentTexture.test.js
git commit -m "Scontorno del capo: maschera dello sfondo dai quattro angoli

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: `garmentTexture.js` — rettangolo del capo e colore dominante

**Files:**
- Modify: `src/utils/garmentTexture.js`
- Test: `src/utils/garmentTexture.test.js`

**Interfaces:**
- Consumes: `backgroundMask` dal Task 1.
- Produces:
  - `garmentBounds(mask, width, height) -> { x, y, width, height } | null`
  - `dominantColor(image, mask) -> string` (esadecimale `#rrggbb`; se `mask` è `null` usa tutti i pixel)
  - `extractGarment(image, opts) -> { ok, bounds, dominantHex, coverage, mask }` — `ok: false` significa "non fidarti della texture, usa la tinta unita". `bounds` è `null` quando `ok` è `false`. `mask` è la maschera già calcolata e **va restituita**: senza, il chiamante che deve rendere trasparente lo sfondo è costretto a rifare da capo il riempimento su tutta l'immagine.

**Regole di degradazione** (esattamente queste, sono il contratto):
- `coverage > 0.92` → lo sfondo non è stato trovato (foto con sfondo movimentato) → `ok: false`
- `coverage < 0.02` → non è stato trovato il capo → `ok: false`
- altrimenti `ok: true`

`coverage` è la frazione di pixel marcati come capo. `dominantHex` è calcolato sui soli pixel del capo quando `ok`, sull'intera immagine altrimenti.

- [ ] **Step 1: Scrivere i test che falliscono**

Aggiungi in coda a `src/utils/garmentTexture.test.js` (riusa `makeImage`, `WHITE`, `BLUE`):

```js
import { garmentBounds, dominantColor, extractGarment } from './garmentTexture';

describe('garmentBounds', () => {
  it('trova il rettangolo che contiene il capo', () => {
    const img = makeImage(10, 10, WHITE, BLUE, { x: 3, y: 2, width: 4, height: 5 });
    const bounds = garmentBounds(backgroundMask(img), 10, 10);
    expect(bounds).toEqual({ x: 3, y: 2, width: 4, height: 5 });
  });

  it('restituisce null se non c’è nessun pixel di capo', () => {
    const img = makeImage(5, 5, WHITE, WHITE, null);
    expect(garmentBounds(backgroundMask(img), 5, 5)).toBeNull();
  });
});

describe('dominantColor', () => {
  it('ignora lo sfondo e restituisce il colore del capo', () => {
    // sfondo bianco largo, capo blu piccolo: senza maschera vincerebbe il bianco
    const img = makeImage(10, 10, WHITE, BLUE, { x: 4, y: 4, width: 2, height: 2 });
    const mask = backgroundMask(img);
    expect(dominantColor(img, mask)).toBe('#284f7f');
  });

  it('senza maschera considera tutti i pixel', () => {
    const img = makeImage(10, 10, WHITE, BLUE, { x: 4, y: 4, width: 2, height: 2 });
    expect(dominantColor(img, null)).toBe('#ffffff');
  });
});

describe('extractGarment', () => {
  it('riesce su una foto normale: sfondo uniforme, capo al centro', () => {
    const img = makeImage(10, 10, WHITE, BLUE, { x: 3, y: 3, width: 4, height: 4 });
    const out = extractGarment(img);
    expect(out.ok).toBe(true);
    expect(out.bounds).toEqual({ x: 3, y: 3, width: 4, height: 4 });
    expect(out.dominantHex).toBe('#284f7f');
    expect(out.coverage).toBeCloseTo(0.16, 5);
  });

  it('degrada se lo sfondo non viene trovato (quasi tutto è "capo")', () => {
    // Ogni pixel diverso dal vicino oltre la tolleranza: il riempimento non parte.
    const width = 10;
    const height = 10;
    const data = new Uint8ClampedArray(width * height * 4);
    for (let p = 0; p < width * height; p++) {
      const i = p * 4;
      data[i] = (p * 97) % 256;
      data[i + 1] = (p * 57) % 256;
      data[i + 2] = (p * 31) % 256;
      data[i + 3] = 255;
    }
    const out = extractGarment({ data, width, height });
    expect(out.ok).toBe(false);
    expect(out.bounds).toBeNull();
    expect(out.dominantHex).toMatch(/^#[0-9a-f]{6}$/);
  });

  it('degrada se non trova nessun capo', () => {
    const img = makeImage(10, 10, WHITE, WHITE, null);
    const out = extractGarment(img);
    expect(out.ok).toBe(false);
    expect(out.bounds).toBeNull();
    expect(out.dominantHex).toBe('#ffffff');
  });
});
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `npx vitest run src/utils/garmentTexture.test.js`
Expected: FAIL — `garmentBounds is not a function`.

- [ ] **Step 3: Scrivere l'implementazione**

Aggiungi in coda a `src/utils/garmentTexture.js`:

```js
/** Rettangolo minimo che contiene i pixel del capo. */
export function garmentBounds(mask, width, height) {
  let minX = width;
  let minY = height;
  let maxX = -1;
  let maxY = -1;

  for (let y = 0; y < height; y++) {
    for (let x = 0; x < width; x++) {
      if (!mask[y * width + x]) continue;
      if (x < minX) minX = x;
      if (x > maxX) maxX = x;
      if (y < minY) minY = y;
      if (y > maxY) maxY = y;
    }
  }
  if (maxX < 0) return null;
  return { x: minX, y: minY, width: maxX - minX + 1, height: maxY - minY + 1 };
}

const toHex = (n) => Math.round(n).toString(16).padStart(2, '0');

/**
 * Colore dominante per istogramma quantizzato: si raggruppano i pixel in bucket
 * da 4 bit per canale, si prende il bucket più popolato e si media sui suoi
 * pixel reali. Deterministico — a differenza di un k-means con centroidi casuali.
 */
export function dominantColor({ data, width, height }, mask) {
  const counts = new Map();
  for (let p = 0; p < width * height; p++) {
    if (mask && !mask[p]) continue;
    const i = p * 4;
    const key =
      ((data[i] >> 4) << 8) | ((data[i + 1] >> 4) << 4) | (data[i + 2] >> 4);
    counts.set(key, (counts.get(key) || 0) + 1);
  }
  if (counts.size === 0) return '#000000';

  let best = null;
  let bestCount = -1;
  for (const [key, count] of counts) {
    if (count > bestCount) {
      best = key;
      bestCount = count;
    }
  }

  let r = 0;
  let g = 0;
  let b = 0;
  let n = 0;
  for (let p = 0; p < width * height; p++) {
    if (mask && !mask[p]) continue;
    const i = p * 4;
    const key =
      ((data[i] >> 4) << 8) | ((data[i + 1] >> 4) << 4) | (data[i + 2] >> 4);
    if (key !== best) continue;
    r += data[i];
    g += data[i + 1];
    b += data[i + 2];
    n++;
  }
  return `#${toHex(r / n)}${toHex(g / n)}${toHex(b / n)}`;
}

/** Soglie oltre le quali non ci si fida della maschera. */
const MAX_COVERAGE = 0.92;
const MIN_COVERAGE = 0.02;

/**
 * Da foto a capo utilizzabile. `ok: false` significa: niente texture, vesti il
 * capo di tinta unita col colore dominante. Brutto no, sbagliato mai.
 */
export function extractGarment(image, opts) {
  const { width, height } = image;
  const mask = backgroundMask(image, opts);
  let garmentPixels = 0;
  for (let p = 0; p < mask.length; p++) garmentPixels += mask[p];
  const coverage = garmentPixels / (width * height);

  if (coverage > MAX_COVERAGE || coverage < MIN_COVERAGE) {
    return {
      ok: false,
      bounds: null,
      dominantHex: dominantColor(image, null),
      coverage,
      mask,
    };
  }
  return {
    ok: true,
    bounds: garmentBounds(mask, width, height),
    dominantHex: dominantColor(image, mask),
    coverage,
    mask,
  };
}
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `npx vitest run src/utils/garmentTexture.test.js`
Expected: PASS, 11 test in totale.

Poi la suite intera: `npm test`
Expected: PASS, 58 test preesistenti + 11 nuovi.

- [ ] **Step 5: Commit**

```bash
git add src/utils/garmentTexture.js src/utils/garmentTexture.test.js
git commit -m "Scontorno del capo: rettangolo, colore dominante, degradazioni

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: `garmentImage.js` — il guscio che tocca il canvas

**Files:**
- Create: `src/utils/garmentImage.js`

**Interfaces:**
- Consumes: `extractGarment` dal Task 2; `CLOTHING_COLORS` da `src/utils/categories.js`.
- Produces: `loadGarmentTexture(item) -> Promise<GarmentTexture>` dove

```js
/**
 * @typedef {Object} GarmentTexture
 * @property {string|null} textureUrl  PNG dataURL, capo ritagliato, sfondo trasparente. null se non disponibile.
 * @property {string} colorHex         Sempre valorizzato: dominante, o fallback dai colori del capo.
 * @property {'texture'|'flat'} kind   'flat' = disegna in tinta unita.
 * @property {string|null} reason      'cors' | 'no-photo' | 'degraded' | null
 */
```

Non ha test unitari: tocca `Image` e `canvas`, che in ambiente `node` non esistono. La sua correttezza si verifica a schermo nel Task 9. Per questo deve restare **sottile**: nessuna logica decisionale che non sia già in `garmentTexture.js`.

- [ ] **Step 1: Scrivere l'implementazione**

Crea `src/utils/garmentImage.js`:

```js
/**
 * Guscio browser attorno a garmentTexture.js: porta la foto in un canvas,
 * chiama la logica pura, restituisce un PNG ritagliato con sfondo trasparente.
 *
 * Le foto dei capi aggiunti da link stanno su domini esterni: leggerne i pixel
 * fa scattare la protezione CORS del browser e il canvas diventa illeggibile.
 * In quel caso si ripiega sulla tinta unita, come già fa il try-on Gemini.
 */
import { extractGarment } from './garmentTexture';
import { CLOTHING_COLORS } from './categories';

const FALLBACK_HEX = '#cfc7bb';

/** Colore di ripiego dai colori dichiarati del capo (sempre presenti nel DB). */
export const garmentFallbackHex = (item) =>
  CLOTHING_COLORS.find((c) => c.id === item?.colors?.[0])?.hex || FALLBACK_HEX;

const loadImage = (src) =>
  new Promise((resolve, reject) => {
    const img = new Image();
    img.crossOrigin = 'anonymous';
    img.onload = () => resolve(img);
    img.onerror = () => reject(new Error('load'));
    img.src = src;
  });

/**
 * Ritaglia `bounds` e rende trasparenti i pixel di sfondo.
 * `mask` arriva già calcolata da extractGarment: ricalcolarla qui vorrebbe dire
 * ripetere il riempimento su tutta l'immagine per ogni capo.
 */
const cutout = (imageData, mask, bounds) => {
  const { data } = imageData;
  for (let p = 0; p < mask.length; p++) {
    if (!mask[p]) data[p * 4 + 3] = 0;
  }
  const full = document.createElement('canvas');
  full.width = imageData.width;
  full.height = imageData.height;
  full.getContext('2d').putImageData(imageData, 0, 0);

  const out = document.createElement('canvas');
  out.width = bounds.width;
  out.height = bounds.height;
  out
    .getContext('2d')
    .drawImage(
      full,
      bounds.x,
      bounds.y,
      bounds.width,
      bounds.height,
      0,
      0,
      bounds.width,
      bounds.height
    );
  return out.toDataURL('image/png');
};

export async function loadGarmentTexture(item) {
  const flat = (reason) => ({
    textureUrl: null,
    colorHex: garmentFallbackHex(item),
    kind: 'flat',
    reason,
  });

  if (!item?.photo) return flat('no-photo');

  let img;
  try {
    img = await loadImage(item.photo);
  } catch {
    return flat('cors');
  }

  const canvas = document.createElement('canvas');
  canvas.width = img.naturalWidth;
  canvas.height = img.naturalHeight;
  const ctx = canvas.getContext('2d', { willReadFrequently: true });
  ctx.drawImage(img, 0, 0);

  let imageData;
  try {
    imageData = ctx.getImageData(0, 0, canvas.width, canvas.height);
  } catch {
    // canvas "sporcato" da un'immagine cross-origin senza header CORS
    return flat('cors');
  }

  const result = extractGarment(imageData);
  if (!result.ok) {
    return { textureUrl: null, colorHex: result.dominantHex, kind: 'flat', reason: 'degraded' };
  }

  return {
    textureUrl: cutout(imageData, result.mask, result.bounds),
    colorHex: result.dominantHex,
    kind: 'texture',
    reason: null,
  };
}
```

- [ ] **Step 2: Verificare che la suite resti verde**

Run: `npm test`
Expected: PASS, 69 test. (Il modulo non è importato da nessun test: si controlla solo di non aver rotto nulla.)

- [ ] **Step 3: Verificare che il build passi**

Run: `npm run build`
Expected: build completato senza errori.

- [ ] **Step 4: Commit**

```bash
git add src/utils/garmentImage.js
git commit -m "Guscio canvas: dalla foto del capo al PNG scontornato

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: modalità piatta — `AvatarSvg` disegna il capo intero

**Files:**
- Modify: `src/components/Avatar/AvatarSvg.jsx`

**Interfaces:**
- Consumes: `garmentLayers(outfit)` da `tryonComposer.js` (invariato); una mappa `textures` di tipo `Record<itemId, GarmentTexture>` passata come prop.
- Produces: `<AvatarSvg config outfit textures height className />`. La prop `textures` è opzionale: se manca, ogni capo si disegna in tinta unita.

**La correzione, in una riga:** oggi la foto viene infilata in una `clipPath` con `preserveAspectRatio="xMidYMid slice"`, che di una foto quadrata dentro un riquadro 68×238 conserva il 28% centrale. Domani il capo è già scontornato e trasparente, quindi si disegna **intero** sopra il corpo, ancorato in alto (`xMidYMin meet`), senza `clipPath`. Le sagome di `GARMENT_SHAPES` restano solo per il caso in tinta unita.

**Riquadri di ancoraggio** (nuovi, sostituiscono l'uso di `box` per le foto — `box` resta per la tinta unita). Coordinate nel viewBox 200×400:

```js
const GARMENT_ANCHORS = {
  top:       { x: 48, y: 60,  width: 104, height: 100 },
  dress:     { x: 50, y: 60,  width: 100, height: 240 },
  bottom:    { x: 62, y: 128, width: 76,  height: 232 },
  outerwear: { x: 40, y: 56,  width: 120, height: 155 },
  shoes:     { x: 74, y: 350, width: 52,  height: 30 },
};
```

- [ ] **Step 1: Sostituire `renderGarment`**

In `src/components/Avatar/AvatarSvg.jsx`, aggiungi la costante `GARMENT_ANCHORS` sopra (dopo `GARMENT_SHAPES`), cambia la firma del componente in

```js
export default function AvatarSvg({ config, outfit, textures, height = 320, className = '' }) {
```

e sostituisci l'intera funzione `renderGarment` con:

```js
  const renderGarment = ({ kind, item }) => {
    const shape = GARMENT_SHAPES[kind];
    if (!shape) return null;
    const texture = textures?.[item.id];

    // Capo scontornato: si appoggia sul corpo intero, senza ritagli.
    if (texture?.textureUrl) {
      return (
        <image
          key={kind}
          href={texture.textureUrl}
          {...GARMENT_ANCHORS[kind]}
          preserveAspectRatio="xMidYMin meet"
        />
      );
    }

    // Ripiego: la sagoma dell'indumento in tinta unita.
    const clipId = `${uid}-${kind}`;
    const fill = texture?.colorHex || '#cfc7bb';
    return (
      <g key={kind}>
        <clipPath id={clipId}>
          {shape.paths.map((d, i) => (
            <path key={i} d={d} />
          ))}
        </clipPath>
        <g clipPath={`url(#${clipId})`}>
          <rect {...shape.box} fill={fill} />
        </g>
        {shape.paths.map((d, i) => (
          <path key={`o${i}`} d={d} fill="none" stroke="rgba(26, 26, 26, 0.18)" strokeWidth="1" />
        ))}
      </g>
    );
  };
```

- [ ] **Step 2: Verificare che la suite resti verde e il build passi**

Run: `npm test && npm run build`
Expected: PASS, 69 test; build senza errori.

- [ ] **Step 3: Commit**

```bash
git add src/components/Avatar/AvatarSvg.jsx
git commit -m "Modalita' piatta: il capo scontornato si appoggia intero sul corpo

Via la clipPath e il ritaglio 'slice' che di una foto quadrata dentro un
riquadro 68x238 conservava solo il 28% centrale, sfondo compreso. Le sagome
restano per il caso degradato, in tinta unita.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: `avatarMesh.js` — il corpo, generato da codice

**Files:**
- Create: `src/utils/avatarMesh.js`
- Test: `src/utils/avatarMesh.test.js`

**Interfaces:**
- Consumes: `getSkinHex`, `getHairHex`, `getBodyWidthFactor` da `src/utils/avatarOptions.js`.
- Produces: `bodyProfiles(config) -> BodyProfiles`

```js
/**
 * Un "profilo" è la lista di coppie [raggio, altezza] che, ruotata attorno
 * all'asse verticale, genera un solido (three.js: LatheGeometry).
 * @typedef {Object} BodyProfiles
 * @property {[number, number][]} torso
 * @property {[number, number][]} leg
 * @property {[number, number][]} arm
 * @property {{ radius: number, y: number }} head
 * @property {number} legOffsetX   scostamento laterale di ciascuna gamba
 * @property {number} armOffsetX   scostamento laterale di ciascun braccio
 * @property {string} skinHex
 * @property {string} hairHex
 * @property {string} hairStyle
 * @property {number} widthFactor
 */
```

Unità: metri simbolici, figura alta 1.8, piedi a `y = 0`, testa in cima. Il `widthFactor` moltiplica **solo i raggi e gli scostamenti laterali**, mai le altezze, e mai il raggio della testa: come nell'SVG, la testa non si allarga con la corporatura.

- [ ] **Step 1: Scrivere il test che fallisce**

Crea `src/utils/avatarMesh.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { bodyProfiles } from './avatarMesh';

const cfg = (bodyShape) => ({
  bodyShape,
  skinTone: 'medium',
  hairColor: 'brown',
  hairStyle: 'medium',
});

const maxRadius = (profile) => Math.max(...profile.map(([r]) => r));

describe('bodyProfiles', () => {
  it('produce profili non vuoti con raggi e altezze positive', () => {
    const p = bodyProfiles(cfg('average'));
    expect(p.torso.length).toBeGreaterThan(3);
    expect(p.leg.length).toBeGreaterThan(3);
    expect(p.arm.length).toBeGreaterThan(1);
    for (const [r, y] of p.torso) {
      expect(r).toBeGreaterThan(0);
      expect(y).toBeGreaterThanOrEqual(0);
    }
  });

  it('la corporatura allarga il torso: plus è più largo di slim', () => {
    expect(maxRadius(bodyProfiles(cfg('plus')).torso)).toBeGreaterThan(
      maxRadius(bodyProfiles(cfg('slim')).torso)
    );
  });

  it('la corporatura scala i raggi esattamente come widthFactor', () => {
    const slim = bodyProfiles(cfg('slim'));
    const average = bodyProfiles(cfg('average'));
    expect(slim.widthFactor).toBeCloseTo(0.88, 5);
    expect(maxRadius(slim.torso) / maxRadius(average.torso)).toBeCloseTo(0.88, 5);
  });

  it('la testa non si allarga con la corporatura', () => {
    expect(bodyProfiles(cfg('plus')).head.radius).toBeCloseTo(
      bodyProfiles(cfg('slim')).head.radius,
      5
    );
  });

  it('non altera le altezze: la testa sta sempre alla stessa quota', () => {
    expect(bodyProfiles(cfg('plus')).head.y).toBeCloseTo(
      bodyProfiles(cfg('slim')).head.y,
      5
    );
  });

  it('riporta i colori risolti da avatarOptions', () => {
    const p = bodyProfiles(cfg('average'));
    expect(p.skinHex).toBe('#C99A6E');
    expect(p.hairHex).toBe('#6B4A2F');
    expect(p.hairStyle).toBe('medium');
  });

  it('con config assente usa i valori di default senza esplodere', () => {
    expect(() => bodyProfiles(undefined)).not.toThrow();
    expect(bodyProfiles(undefined).widthFactor).toBeCloseTo(1, 5);
  });
});
```

- [ ] **Step 2: Eseguire il test e verificare che fallisca**

Run: `npx vitest run src/utils/avatarMesh.test.js`
Expected: FAIL — `Failed to resolve import "./avatarMesh"`.

- [ ] **Step 3: Scrivere l'implementazione**

Crea `src/utils/avatarMesh.js`:

```js
/**
 * Il corpo dell'avatar, generato da codice: nessun modello 3D da scaricare,
 * nessuna licenza di terzi, e la corporatura resta un parametro invece di
 * dipendere dai morph target che il modello potrebbe non avere.
 *
 * Un profilo è una lista di [raggio, altezza]: ruotandolo attorno all'asse
 * verticale si ottiene il solido (three.js: LatheGeometry). Puro, senza three.js:
 * così è testabile in ambiente node.
 */
import { getSkinHex, getHairHex, getBodyWidthFactor } from './avatarOptions';

/** Figura alta 1.8, piedi a y = 0. */
const HEAD_RADIUS = 0.115;
const HEAD_Y = 1.63;

// [raggio, altezza] a corporatura 1
const TORSO = [
  [0.005, 1.5],
  [0.075, 1.47],
  [0.09, 1.42],
  [0.145, 1.3],
  [0.135, 1.18],
  [0.105, 1.08],
  [0.125, 0.99],
  [0.15, 0.93],
  [0.145, 0.88],
  [0.005, 0.86],
];

const LEG = [
  [0.005, 0.9],
  [0.085, 0.86],
  [0.075, 0.6],
  [0.05, 0.45],
  [0.06, 0.3],
  [0.04, 0.08],
  [0.05, 0.02],
  [0.005, 0.0],
];

const ARM = [
  [0.005, 1.42],
  [0.042, 1.38],
  [0.032, 1.1],
  [0.026, 0.92],
  [0.005, 0.88],
];

const LEG_OFFSET_X = 0.062;
const ARM_OFFSET_X = 0.165;

const scaleRadii = (profile, factor) => profile.map(([r, y]) => [r * factor, y]);

export function bodyProfiles(config) {
  const widthFactor = getBodyWidthFactor(config?.bodyShape);
  return {
    torso: scaleRadii(TORSO, widthFactor),
    leg: scaleRadii(LEG, widthFactor),
    arm: scaleRadii(ARM, widthFactor),
    head: { radius: HEAD_RADIUS, y: HEAD_Y },
    legOffsetX: LEG_OFFSET_X * widthFactor,
    armOffsetX: ARM_OFFSET_X * widthFactor,
    skinHex: getSkinHex(config?.skinTone),
    hairHex: getHairHex(config?.hairColor),
    hairStyle: config?.hairStyle || 'medium',
    widthFactor,
  };
}
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `npx vitest run src/utils/avatarMesh.test.js`
Expected: PASS, 7 test.

- [ ] **Step 5: Commit**

```bash
git add src/utils/avatarMesh.js src/utils/avatarMesh.test.js
git commit -m "Corpo 3D generato da codice: profili di rotazione parametrici

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: `garmentMesh.js` — i cinque capi, come guscio del corpo

**Files:**
- Create: `src/utils/garmentMesh.js`
- Test: `src/utils/garmentMesh.test.js`

**Interfaces:**
- Consumes: `bodyProfiles(config)` dal Task 5.
- Produces:
  - `GARMENT_KINDS` — array `['top', 'dress', 'bottom', 'outerwear', 'shoes']`, gli stessi `kind` che `garmentLayers()` produce.
  - `garmentProfile(kind, config) -> { profile: [number, number][], offsetX: number, doubled: boolean } | null` — `doubled: true` significa che la mesh va istanziata due volte, a `±offsetX` (gambe, scarpe). `null` per un `kind` sconosciuto.
  - `radiusAt(profile, y) -> number` — raggio del profilo alla quota `y`, per interpolazione lineare. Esportata perché è ciò che rende verificabile l'invariante "il capo sta fuori dalla pelle".
  - `GARMENT_GAP = 0.012` — di quanto il capo sta fuori dalla pelle.

**Attenzione a come si formula l'invariante.** "Il raggio massimo del capo supera il raggio massimo del corpo" è **falso** e il test fallirebbe: il punto più largo del torso sono i fianchi (raggio 0.15 a quota 0.93), che stanno *sotto* la campata di una maglietta (0.98–1.45), dove il corpo arriva a 0.145. L'invariante vera è locale: **a ogni quota**, il raggio del capo supera quello del corpo. Per questo `radiusAt` va esportata.

Il capo si ricava dal profilo del corpo, ingrossato di `GARMENT_GAP` e tagliato all'altezza giusta. Così **aderisce alla corporatura scelta** senza che nessuno debba modellare cinque capi per cinque corporature.

- [ ] **Step 1: Scrivere il test che fallisce**

Crea `src/utils/garmentMesh.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { garmentProfile, radiusAt, GARMENT_KINDS, GARMENT_GAP } from './garmentMesh';
import { bodyProfiles } from './avatarMesh';

const cfg = { bodyShape: 'average', skinTone: 'medium', hairColor: 'brown', hairStyle: 'medium' };

const yRange = (profile) => {
  const ys = profile.map(([, y]) => y);
  return [Math.min(...ys), Math.max(...ys)];
};

describe('garmentProfile', () => {
  it('conosce i cinque tipi che garmentLayers produce', () => {
    expect(GARMENT_KINDS).toEqual(['top', 'dress', 'bottom', 'outerwear', 'shoes']);
    for (const kind of GARMENT_KINDS) {
      expect(garmentProfile(kind, cfg)).not.toBeNull();
    }
  });

  it('restituisce null per un tipo sconosciuto', () => {
    expect(garmentProfile('cappello', cfg)).toBeNull();
  });

  it('il capo sta fuori dalla pelle a ogni quota, mai dentro', () => {
    // NON "il raggio massimo del capo supera il raggio massimo del corpo": i
    // fianchi (0.15 a quota 0.93) stanno sotto la campata del top (0.98-1.45),
    // dove il torso arriva a 0.145. L'invariante e' locale, quota per quota.
    const body = bodyProfiles(cfg);
    const { profile } = garmentProfile('top', cfg);
    const shell = profile.slice(1, -1); // via i due punti di chiusura sull'asse
    expect(shell.length).toBeGreaterThan(10);
    for (const [r, y] of shell) {
      expect(r).toBeCloseTo(radiusAt(body.torso, y) + GARMENT_GAP, 9);
    }
  });

  it('il capospalla sta fuori del top: si indossa sopra', () => {
    const top = garmentProfile('top', cfg);
    const outer = garmentProfile('outerwear', cfg);
    const y = 1.2; // quota coperta da entrambi
    const radiusOf = ({ profile }) => radiusAt(profile.slice(1, -1), y);
    expect(radiusOf(outer)).toBeGreaterThan(radiusOf(top));
  });

  it('il capo segue la corporatura: plus è più largo di slim', () => {
    const slim = garmentProfile('top', { ...cfg, bodyShape: 'slim' });
    const plus = garmentProfile('top', { ...cfg, bodyShape: 'plus' });
    expect(Math.max(...plus.profile.map(([r]) => r))).toBeGreaterThan(
      Math.max(...slim.profile.map(([r]) => r))
    );
  });

  it("l'abito scende più in basso del top", () => {
    const [topLow] = yRange(garmentProfile('top', cfg).profile);
    const [dressLow] = yRange(garmentProfile('dress', cfg).profile);
    expect(dressLow).toBeLessThan(topLow);
  });

  it('il bottom parte dalla vita e arriva alle caviglie', () => {
    const [low, high] = yRange(garmentProfile('bottom', cfg).profile);
    expect(high).toBeCloseTo(0.95, 1);
    expect(low).toBeLessThan(0.15);
  });

  it('bottom e scarpe si istanziano due volte, il top no', () => {
    expect(garmentProfile('bottom', cfg).doubled).toBe(true);
    expect(garmentProfile('shoes', cfg).doubled).toBe(true);
    expect(garmentProfile('top', cfg).doubled).toBe(false);
    expect(garmentProfile('dress', cfg).doubled).toBe(false);
  });

  it('i cinque tipi producono profili distinti', () => {
    const seen = GARMENT_KINDS.map((k) => JSON.stringify(garmentProfile(k, cfg).profile));
    expect(new Set(seen).size).toBe(5);
  });
});
```

- [ ] **Step 2: Eseguire il test e verificare che fallisca**

Run: `npx vitest run src/utils/garmentMesh.test.js`
Expected: FAIL — `Failed to resolve import "./garmentMesh"`.

- [ ] **Step 3: Scrivere l'implementazione**

Crea `src/utils/garmentMesh.js`:

```js
/**
 * I capi 3D: gusci ricavati dal profilo del corpo, ingrossati di GARMENT_GAP e
 * tagliati all'altezza giusta. Derivarli dal corpo significa che aderiscono da
 * soli alla corporatura scelta: nessuno deve modellare 5 capi x 5 corporature.
 *
 * La forma è generica — una maglietta è "una maglietta". Colore e fantasia
 * vengono dalla foto vera (garmentTexture.js). Il capo esatto addosso alla
 * persona lo dà solo la scheda "Sulla tua foto (AI)".
 */
import { bodyProfiles } from './avatarMesh';

export const GARMENT_KINDS = ['top', 'dress', 'bottom', 'outerwear', 'shoes'];

/** Di quanto il capo sta fuori dalla pelle. */
export const GARMENT_GAP = 0.012;

/** Estremi verticali di ogni capo, in unità della figura alta 1.8. */
const SPANS = {
  top: { from: 0.98, to: 1.45, source: 'torso', gap: GARMENT_GAP },
  dress: { from: 0.55, to: 1.45, source: 'torso', gap: GARMENT_GAP },
  bottom: { from: 0.06, to: 0.95, source: 'leg', gap: GARMENT_GAP },
  outerwear: { from: 0.9, to: 1.46, source: 'torso', gap: GARMENT_GAP * 2.2 },
  shoes: { from: 0.0, to: 0.1, source: 'leg', gap: GARMENT_GAP * 1.6 },
};

const DOUBLED = new Set(['bottom', 'shoes']);

/**
 * Raggio del profilo alla quota y, per interpolazione lineare.
 * Esportata: è ciò che permette al test di verificare, quota per quota, che il
 * capo stia fuori dalla pelle.
 */
export const radiusAt = (profile, y) => {
  const sorted = [...profile].sort((a, b) => a[1] - b[1]);
  if (y <= sorted[0][1]) return sorted[0][0];
  if (y >= sorted[sorted.length - 1][1]) return sorted[sorted.length - 1][0];
  for (let i = 1; i < sorted.length; i++) {
    const [r0, y0] = sorted[i - 1];
    const [r1, y1] = sorted[i];
    if (y <= y1) {
      const k = y1 === y0 ? 0 : (y - y0) / (y1 - y0);
      return r0 + (r1 - r0) * k;
    }
  }
  return sorted[sorted.length - 1][0];
};

const STEPS = 14;

export function garmentProfile(kind, config) {
  const span = SPANS[kind];
  if (!span) return null;

  const body = bodyProfiles(config);
  const source = body[span.source];
  const profile = [];

  // Bordo inferiore chiuso, poi il guscio, poi bordo superiore chiuso:
  // un solido, non una superficie aperta.
  profile.push([0.001, span.from]);
  for (let i = 0; i <= STEPS; i++) {
    const y = span.from + ((span.to - span.from) * i) / STEPS;
    profile.push([radiusAt(source, y) + span.gap, y]);
  }
  profile.push([0.001, span.to]);

  return {
    profile,
    offsetX: DOUBLED.has(kind) ? body.legOffsetX : 0,
    doubled: DOUBLED.has(kind),
  };
}
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `npx vitest run src/utils/garmentMesh.test.js`
Expected: PASS, 9 test.

Poi: `npm test`
Expected: PASS, 85 test.

- [ ] **Step 5: Commit**

```bash
git add src/utils/garmentMesh.js src/utils/garmentMesh.test.js
git commit -m "Capi 3D come guscio del corpo: aderiscono alla corporatura scelta

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: `webgl.js` e la dipendenza three.js

**Files:**
- Create: `src/utils/webgl.js`
- Modify: `package.json`

**Interfaces:**
- Produces: `hasWebGL() -> boolean`. In ambiente senza `document` (test, SSR) restituisce `false`.

- [ ] **Step 1: Installare three.js**

Run: `npm install three@^0.180.0`
Expected: `three` compare in `dependencies` di `package.json`.

- [ ] **Step 2: Scrivere `src/utils/webgl.js`**

```js
/**
 * WebGL non c'è sempre: vecchi telefoni, accelerazione disattivata, modalità
 * risparmio energetico. In quel caso la modalità 3D si disabilita e si spiega
 * perché, invece di mostrare una tela nera.
 */
let cached = null;

export function hasWebGL() {
  if (cached !== null) return cached;
  if (typeof document === 'undefined') {
    cached = false;
    return cached;
  }
  try {
    const canvas = document.createElement('canvas');
    cached = Boolean(
      canvas.getContext('webgl2') || canvas.getContext('webgl')
    );
  } catch {
    cached = false;
  }
  return cached;
}
```

- [ ] **Step 3: Verificare suite e build**

Run: `npm test && npm run build`
Expected: PASS, 85 test; build senza errori.

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json src/utils/webgl.js
git commit -m "three.js come dipendenza e rilevamento del supporto WebGL

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: `Avatar3D.jsx` — la scena

**Files:**
- Create: `src/components/Avatar/Avatar3D.jsx`

**Interfaces:**
- Consumes: `bodyProfiles` (Task 5), `garmentProfile` + `GARMENT_KINDS` (Task 6), `garmentLayers` da `tryonComposer.js`, la mappa `textures` (Task 3).
- Produces: `<Avatar3D config outfit textures height />` — **export default**, così `React.lazy(() => import('./Avatar3D'))` funziona.

**Come si ottiene "fantasia davanti, colore dietro":** la mesh del capo è in tinta unita col colore dominante. Sopra il davanti si aggiunge una **seconda superficie curva** (un settore di cilindro che abbraccia circa 140° del fronte), texturizzata col PNG scontornato e trasparente. Ruotando, la fantasia c'è dove la si guarda e i fianchi chiudono in tinta: nessuna texture stirata, nessuna ripetizione.

**La fantasia serve anche ai capi sdoppiati.** Pantaloni e scarpe si istanziano due volte (una per gamba), ma la superficie della fantasia resta **una sola**, allargata di `offsetX` fino ad abbracciare entrambe le gambe: un paio di jeans è una foto sola che copre due gambe, non la stessa foto stampata due volte. Senza questa accortezza i bottom uscirebbero in tinta unita e la fantasia si vedrebbe solo dalla vita in su.

**Rendering su richiesta:** niente `requestAnimationFrame` continuo. Si ridisegna quando cambia l'outfit e durante il trascinamento. Sul telefono deve consumare batteria solo mentre lo si guarda.

- [ ] **Step 1: Scrivere il componente**

Crea `src/components/Avatar/Avatar3D.jsx`:

```js
import { useEffect, useRef } from 'react';
import {
  Scene,
  PerspectiveCamera,
  WebGLRenderer,
  LatheGeometry,
  CylinderGeometry,
  SphereGeometry,
  MeshStandardMaterial,
  Mesh,
  Group,
  Vector2,
  Color,
  AmbientLight,
  DirectionalLight,
  TextureLoader,
  DoubleSide,
  SRGBColorSpace,
} from 'three';
import { bodyProfiles } from '../../utils/avatarMesh';
import { garmentProfile } from '../../utils/garmentMesh';
import { garmentLayers } from '../../utils/tryonComposer';

const RADIAL_SEGMENTS = 32;
const DECAL_ARC = (140 * Math.PI) / 180;

const lathe = (profile) =>
  new LatheGeometry(
    profile.map(([r, y]) => new Vector2(Math.max(r, 0.001), y)),
    RADIAL_SEGMENTS
  );

/**
 * Superficie curva sul davanti del capo, dove si applica la fantasia vera.
 * Serve anche ai capi sdoppiati (pantaloni, scarpe): senza, uscirebbero in
 * tinta unita e la fantasia si vedrebbe solo sulla metà superiore del corpo.
 */
const frontDecal = (profile, textureUrl, onReady, extraRadius = 0) => {
  const ys = profile.map(([, y]) => y);
  const top = Math.max(...ys);
  const bottom = Math.min(...ys);
  const radius = Math.max(...profile.map(([r]) => r)) + extraRadius + 0.004;

  const geometry = new CylinderGeometry(
    radius,
    radius,
    top - bottom,
    24,
    1,
    true,
    -DECAL_ARC / 2,
    DECAL_ARC
  );
  const material = new MeshStandardMaterial({
    transparent: true,
    side: DoubleSide,
    roughness: 0.9,
  });
  const texture = new TextureLoader().load(textureUrl, onReady);
  texture.colorSpace = SRGBColorSpace;
  material.map = texture;

  const mesh = new Mesh(geometry, material);
  mesh.position.y = (top + bottom) / 2;
  mesh.rotation.y = Math.PI; // il fronte guarda la camera
  return mesh;
};

export default function Avatar3D({ config, outfit, textures, height = 420 }) {
  const mountRef = useRef(null);
  const stateRef = useRef({});

  useEffect(() => {
    const mount = mountRef.current;
    if (!mount) return undefined;

    const scene = new Scene();
    const camera = new PerspectiveCamera(32, 0.5, 0.1, 100);
    camera.position.set(0, 0.95, 5.2);
    camera.lookAt(0, 0.9, 0);

    const renderer = new WebGLRenderer({ antialias: true, alpha: true });
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
    renderer.setSize(height / 2, height);
    mount.appendChild(renderer.domElement);

    scene.add(new AmbientLight(0xffffff, 1.5));
    const key = new DirectionalLight(0xffffff, 1.8);
    key.position.set(2, 4, 3);
    scene.add(key);
    const rim = new DirectionalLight(0xffffff, 0.5);
    rim.position.set(-3, 2, -2);
    scene.add(rim);

    const figure = new Group();
    scene.add(figure);

    const render = () => renderer.render(scene, camera);
    Object.assign(stateRef.current, { scene, camera, renderer, figure, render });

    // Rotazione al trascinamento: si ridisegna solo mentre il dito si muove.
    let dragging = false;
    let lastX = 0;
    const onDown = (e) => {
      dragging = true;
      lastX = e.clientX;
      renderer.domElement.setPointerCapture(e.pointerId);
    };
    const onMove = (e) => {
      if (!dragging) return;
      figure.rotation.y += (e.clientX - lastX) * 0.01;
      lastX = e.clientX;
      render();
    };
    const onUp = () => {
      dragging = false;
    };
    renderer.domElement.addEventListener('pointerdown', onDown);
    renderer.domElement.addEventListener('pointermove', onMove);
    renderer.domElement.addEventListener('pointerup', onUp);
    renderer.domElement.addEventListener('pointercancel', onUp);
    renderer.domElement.style.touchAction = 'none';
    renderer.domElement.style.cursor = 'grab';

    return () => {
      renderer.domElement.removeEventListener('pointerdown', onDown);
      renderer.domElement.removeEventListener('pointermove', onMove);
      renderer.domElement.removeEventListener('pointerup', onUp);
      renderer.domElement.removeEventListener('pointercancel', onUp);
      renderer.dispose();
      mount.removeChild(renderer.domElement);
    };
  }, [height]);

  // Corpo e capi si ricostruiscono quando cambia la configurazione o l'outfit.
  useEffect(() => {
    const { figure, render } = stateRef.current;
    if (!figure) return;

    while (figure.children.length) {
      const child = figure.children.pop();
      child.geometry?.dispose();
      child.material?.map?.dispose();
      child.material?.dispose();
    }

    const body = bodyProfiles(config);
    const skin = new MeshStandardMaterial({ color: new Color(body.skinHex), roughness: 0.85 });
    const hair = new MeshStandardMaterial({ color: new Color(body.hairHex), roughness: 0.95 });

    figure.add(new Mesh(lathe(body.torso), skin));
    for (const sign of [-1, 1]) {
      const leg = new Mesh(lathe(body.leg), skin);
      leg.position.x = sign * body.legOffsetX;
      figure.add(leg);
      const arm = new Mesh(lathe(body.arm), skin);
      arm.position.x = sign * body.armOffsetX;
      figure.add(arm);
    }

    const head = new Mesh(new SphereGeometry(body.head.radius, 24, 18), skin);
    head.position.y = body.head.y;
    head.scale.set(0.92, 1.12, 0.95);
    figure.add(head);

    if (body.hairStyle !== 'bald') {
      const cap = new Mesh(new SphereGeometry(body.head.radius * 1.06, 24, 18), hair);
      cap.position.y = body.head.y + 0.02;
      cap.scale.set(0.95, 1.0, 0.98);
      figure.add(cap);
    }

    for (const { kind, item } of garmentLayers(outfit)) {
      const shape = garmentProfile(kind, config);
      if (!shape) continue;
      const texture = textures?.[item.id];
      const color = new Color(texture?.colorHex || '#cfc7bb');
      const material = new MeshStandardMaterial({ color, roughness: 0.92 });

      const offsets = shape.doubled ? [-shape.offsetX, shape.offsetX] : [0];
      for (const dx of offsets) {
        const mesh = new Mesh(lathe(shape.profile), material);
        mesh.position.x = dx;
        figure.add(mesh);
      }

      // Una sola superficie per la fantasia, anche sui capi sdoppiati: un paio
      // di jeans è una foto sola che copre due gambe, non la stessa foto due
      // volte. Per i capi sdoppiati la superficie si allarga fino ad abbracciarle.
      if (texture?.textureUrl) {
        figure.add(
          frontDecal(shape.profile, texture.textureUrl, render, shape.offsetX)
        );
      }
    }

    render();
  }, [config, outfit, textures]);

  return <div className="avatar3d" ref={mountRef} style={{ height }} aria-label="avatar 3D" />;
}
```

- [ ] **Step 2: Verificare suite e build**

Run: `npm test && npm run build`
Expected: PASS, 85 test; build senza errori. Nel riepilogo di Vite deve comparire un **chunk separato** per `Avatar3D` — se three.js finisce nel chunk principale, il `React.lazy` del Task 9 non è ancora in piedi e va corretto lì.

- [ ] **Step 3: Commit**

```bash
git add src/components/Avatar/Avatar3D.jsx
git commit -m "Scena 3D: corpo, capi e fantasia vera sul davanti

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: il selettore 3D/Piatto in `OutfitOnAvatar`

**Files:**
- Modify: `src/components/Avatar/OutfitOnAvatar.jsx`
- Modify: `src/components/Avatar/Avatar.css`
- Modify: `src/i18n/it.json`, `src/i18n/en.json`

**Interfaces:**
- Consumes: `hasWebGL()` (Task 7), `loadGarmentTexture(item)` (Task 3), `AvatarSvg` con la prop `textures` (Task 4), `Avatar3D` via `React.lazy` (Task 8).
- Produces: nessuna nuova API pubblica. `OutfitOnAvatar` mantiene la firma `{ outfit, avatarConfig, onSlotClick, onRemove }`.

Preferenza in `localStorage['sv_avatar_render']`, valori `'3d'` | `'flat'`. Default `'3d'` se WebGL c'è, `'flat'` altrimenti. Se WebGL manca, il bottone 3D è `disabled` e sotto compare `avatar.renderNoWebgl`.

- [ ] **Step 1: Aggiungere le chiavi i18n**

In `src/i18n/it.json`, dentro l'oggetto `avatar`:

```json
"render3d": "3D",
"renderFlat": "Piatto",
"render3dHint": "Gratis e illimitata. Ruota la figura col dito. La forma del capo è generica: colore e fantasia sono i tuoi, il taglio no.",
"renderFlatHint": "Gratis e illimitata. Leggera, non consuma batteria. La figura non ruota.",
"renderNoWebgl": "Questo dispositivo non supporta la grafica 3D: resta disponibile la modalità piatta.",
"renderLoading": "Preparo la scena 3D..."
```

In `src/i18n/en.json`, dentro `avatar`:

```json
"render3d": "3D",
"renderFlat": "Flat",
"render3dHint": "Free and unlimited. Spin the figure with your finger. The garment shape is generic: the colour and pattern are yours, the cut is not.",
"renderFlatHint": "Free and unlimited. Light, easy on the battery. The figure does not rotate.",
"renderNoWebgl": "This device does not support 3D graphics: the flat mode is still available.",
"renderLoading": "Preparing the 3D scene..."
```

- [ ] **Step 2: Riscrivere la testa di `OutfitOnAvatar.jsx`**

Sostituisci gli import e l'inizio del componente:

```js
import { lazy, Suspense, useEffect, useMemo, useState } from 'react';
import { useTranslation } from 'react-i18next';
import AvatarSvg from './AvatarSvg';
import { Icon } from '../common';
import { MAX_ACCESSORIES } from '../../utils/tryonComposer';
import { loadGarmentTexture } from '../../utils/garmentImage';
import { hasWebGL } from '../../utils/webgl';
import './Avatar.css';

const Avatar3D = lazy(() => import('./Avatar3D'));

const RENDER_KEY = 'sv_avatar_render';

const readRenderMode = () => {
  if (!hasWebGL()) return 'flat';
  try {
    return localStorage.getItem(RENDER_KEY) === 'flat' ? 'flat' : '3d';
  } catch {
    return '3d';
  }
};

const outfitItems = (outfit) =>
  [outfit.top, outfit.bottom, outfit.shoes, outfit.outerwear].filter(Boolean);
```

Poi, subito dopo `const interactive = typeof onSlotClick === 'function';`, aggiungi:

```js
  const webgl = hasWebGL();
  const [renderMode, setRenderMode] = useState(readRenderMode);
  const [textures, setTextures] = useState({});

  const chooseMode = (mode) => {
    setRenderMode(mode);
    try {
      localStorage.setItem(RENDER_KEY, mode);
    } catch {
      /* modalità privata: la preferenza vale solo per questa sessione */
    }
  };

  // Le texture si calcolano una volta per capo e si riusano finché l'outfit non cambia.
  const itemsKey = outfitItems(outfit || {})
    .map((i) => i.id)
    .join(',');

  useEffect(() => {
    let cancelled = false;
    const items = outfitItems(outfit || {});
    Promise.all(items.map((item) => loadGarmentTexture(item).then((t) => [item.id, t]))).then(
      (entries) => {
        if (!cancelled) setTextures(Object.fromEntries(entries));
      }
    );
    return () => {
      cancelled = true;
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [itemsKey]);
```

- [ ] **Step 3: Sostituire il blocco della figura**

Nel `return`, sostituisci

```js
        <div className="tryon__figure">
          <AvatarSvg config={avatarConfig} outfit={outfit} height={420} />
        </div>
```

con

```js
        <div className="tryon__figure">
          {renderMode === '3d' && webgl ? (
            <Suspense fallback={<p className="tryon__loading sv-label">{t('avatar.renderLoading')}</p>}>
              <Avatar3D
                config={avatarConfig}
                outfit={outfit}
                textures={textures}
                height={420}
              />
            </Suspense>
          ) : (
            <AvatarSvg
              config={avatarConfig}
              outfit={outfit}
              textures={textures}
              height={420}
            />
          )}
        </div>
```

E subito prima di `<div className="tryon__stage">`, inserisci il selettore:

```js
      <div className="tryon__render">
        <div className="tryon__render-tabs" role="group">
          <button
            type="button"
            className={`tryon__render-tab ${renderMode === '3d' ? 'tryon__render-tab--active' : ''}`}
            onClick={() => chooseMode('3d')}
            disabled={!webgl}
            aria-pressed={renderMode === '3d'}
          >
            {t('avatar.render3d')}
          </button>
          <button
            type="button"
            className={`tryon__render-tab ${renderMode === 'flat' ? 'tryon__render-tab--active' : ''}`}
            onClick={() => chooseMode('flat')}
            aria-pressed={renderMode === 'flat'}
          >
            {t('avatar.renderFlat')}
          </button>
        </div>
        <p className="tryon__render-hint sv-label">
          {!webgl
            ? t('avatar.renderNoWebgl')
            : renderMode === '3d'
              ? t('avatar.render3dHint')
              : t('avatar.renderFlatHint')}
        </p>
      </div>
```

- [ ] **Step 4: Aggiungere gli stili**

In coda a `src/components/Avatar/Avatar.css`:

```css
.tryon__render {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--sv-space-2);
  margin-bottom: var(--sv-space-3);
}

.tryon__render-tabs {
  display: inline-flex;
  border: 1px solid var(--sv-border);
}

.tryon__render-tab {
  padding: var(--sv-space-2) var(--sv-space-4);
  background: none;
  border: none;
  font: inherit;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  font-size: 0.72rem;
  cursor: pointer;
  color: var(--sv-ink-soft);
}

.tryon__render-tab--active {
  background: var(--sv-ink);
  color: var(--sv-bg);
}

.tryon__render-tab:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.tryon__render-hint {
  max-width: 34ch;
  text-align: center;
  line-height: 1.5;
  text-transform: none;
  letter-spacing: 0;
}

.avatar3d canvas {
  display: block;
}

.tryon__loading {
  padding: var(--sv-space-6) 0;
}
```

I token usati esistono tutti in `src/styles/tokens.css`: `--sv-border`, `--sv-ink`, `--sv-ink-soft`, `--sv-bg`, `--sv-accent`, `--sv-space-1..8`. Non ce ne sono altri da inventare — in particolare `--sv-line` e `--sv-cream` **non esistono**.

- [ ] **Step 5: Verificare suite e build**

Run: `npm test && npm run build`
Expected: PASS, 85 test; build senza errori, con **chunk separato** contenente three.js.

- [ ] **Step 6: Commit**

```bash
git add src/components/Avatar/OutfitOnAvatar.jsx src/components/Avatar/Avatar.css src/i18n/it.json src/i18n/en.json
git commit -m "Selettore 3D/Piatto: sceglie l'utente, e ogni modalita' dice cosa fa

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: dire quanto costa la scheda AI

**Files:**
- Modify: `src/i18n/it.json`, `src/i18n/en.json`
- Modify: `src/pages/TryOnPage/TryOnPage.jsx`
- Modify: `src/pages/TryOnPage/TryOnPage.css`

**Interfaces:** nessuna nuova API.

Richiesta esplicita di Lorenzo: ogni modalità espone il costo. Oggi `tryon.photoIntro` non nomina il prezzo; il prezzo compare solo nel messaggio d'errore della quota, cioè **dopo** che l'utente ci ha sbattuto la testa.

- [ ] **Step 1: Correggere i testi**

In `src/i18n/it.json`, dentro `tryon`, sostituisci `modeAvatarHint` e `photoIntro` e aggiungi `photoCost`:

```json
"modeAvatarHint": "Gratis: l'avatar indossa i capi scelti, in 3D o in versione piatta.",
"photoIntro": "L'AI di Google genera una fotografia di te che indossi i capi scelti. È l'unica modalità che mostra il tuo capo esatto, ma produce un'immagine ferma: non si ruota.",
"photoCost": "A pagamento: circa 4 centesimi a foto, addebitati sull'account Google di chi possiede la chiave. Serve la fatturazione attiva su aistudio.google.com."
```

In `src/i18n/en.json`, dentro `tryon`:

```json
"modeAvatarHint": "Free: your avatar wears the chosen items, in 3D or flat.",
"photoIntro": "Google's AI generates a photograph of you wearing the chosen items. It is the only mode that shows your exact garment, but it produces a still image: it does not rotate.",
"photoCost": "Paid: about 4 cents per photo, charged to the Google account that owns the key. Billing must be enabled at aistudio.google.com."
```

- [ ] **Step 2: Mostrare il costo prima di generare, non dopo**

In `src/pages/TryOnPage/TryOnPage.jsx`, sotto `<p className="tryon-page__photo-intro">{t('tryon.photoIntro')}</p>` aggiungi:

```jsx
          <p className="tryon-page__photo-cost sv-label">{t('tryon.photoCost')}</p>
```

- [ ] **Step 3: Stile**

In coda a `src/pages/TryOnPage/TryOnPage.css`:

```css
.tryon-page__photo-cost {
  margin-top: var(--sv-space-2);
  padding: var(--sv-space-2) var(--sv-space-3);
  border-left: 2px solid var(--sv-accent);
  text-transform: none;
  letter-spacing: 0;
  line-height: 1.5;
}
```

- [ ] **Step 4: Verificare suite e build**

Run: `npm test && npm run build`
Expected: PASS, 85 test; build senza errori.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/it.json src/i18n/en.json src/pages/TryOnPage/TryOnPage.jsx src/pages/TryOnPage/TryOnPage.css
git commit -m "Ogni modalita' dichiara costo e limiti prima dell'uso, non dopo

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 11: verifica a schermo

**Files:** nessuno modificato (salvo correzioni emerse).

Gli unit test coprono la logica pura. Il 3D, il canvas e il selettore no: vanno **visti**. Questo compito è la prova che il difetto segnalato da Lorenzo è sparito davvero.

- [ ] **Step 1: Preparare lo strumento**

Nella cartella scratchpad di sessione:

```bash
npm init -y && npm install playwright-core
```

Chrome è già installato sul Mac: si usa con `channel: 'chrome'`, senza scaricare browser.

- [ ] **Step 2: Avviare l'app in modalità locale**

La modalità locale precarica i capi di esempio, così il guardaroba non è vuoto:

```bash
mv .env.local .env.local.bak && npm run dev
```

(A fine verifica: `mv .env.local.bak .env.local`.)

**Attenzione:** i `SAMPLE_ITEMS` di `WardrobeContext.jsx` puntano a `picsum.photos`, che restituisce fotografie casuali — non capi, e su dominio esterno. Servono a provare il percorso CORS e la degradazione, **non** l'aspetto. Per giudicare la resa, aggiungi a mano un capo con una vera foto di indumento su sfondo uniforme.

- [ ] **Step 3: Percorso da verificare**

Registrazione → onboarding → aggiungi un capo con foto reale di indumento → `/tryon`.

Su quella pagina, controllare una per una:

1. **Modalità 3D** (default): la figura compare, la si trascina col dito e ruota. Il capo è colorato come la foto e la fantasia si vede sul davanti.
2. Vestire **tutti e cinque** i tipi: top, abito (esclude il bottom), bottom, capospalla, scarpe.
3. **Modalità piatta**: il capo si vede **intero**, non una striscia, e non c'è sfondo dentro le gambe. È la prova che il difetto è chiuso.
4. La preferenza sopravvive al reload (`localStorage['sv_avatar_render']`).
5. **Capo con foto remota** (i `SAMPLE_ITEMS`): niente errore in console, il capo si veste in tinta unita.
6. **Fallback**: avviare Chrome con `--disable-gpu --disable-software-rasterizer`; il bottone 3D è disabilitato e compare `avatar.renderNoWebgl`.
7. **Scheda "Sulla tua foto (AI)"**: il costo (~4 centesimi a foto) si legge **prima** di premere il bottone.
8. **Zero errori in console** su tutti i passaggi.
9. Screenshot di: 3D vestito, 3D ruotato di ~90°, modalità piatta, fallback senza WebGL.

- [ ] **Step 4: Confronto con il difetto originale**

Rimettere accanto lo screenshot del difetto (foto di jeans ridotta a striscia dentro le gambe piene di sfondo) e quello nuovo della modalità piatta. Devono raccontare la stessa scena con esiti opposti. Se non lo fanno, il difetto **non è chiuso**: tornare al Task 4.

- [ ] **Step 5: Prova su telefono vero**

Aprire `http://<ip-del-mac>:5173/` dal telefono (`npm run dev -- --host`): la rotazione deve essere fluida e la scena non deve scaldare il dispositivo. È l'unico modo di sapere se la scelta del 3D regge dove l'app viene usata davvero.

- [ ] **Step 6: Aggiornare `MEMORIA.md` e pushare**

Aggiungere a `MEMORIA.md` la sezione dell'avatar 3D: cosa c'è, la causa del difetto dei vestiti, le due modalità, il fallback, e il fatto che il proxy Gemini è il prossimo progetto (§9 della spec).

```bash
git add MEMORIA.md && git commit -m "MEMORIA: avatar 3D e capi scontornati, prossimo passo il proxy Gemini

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
git push
```

Il push su `main` fa da solo test, build e deploy su GitHub Pages.

---

## Autoverifica del piano

**Copertura della spec.** §3 architettura → Task 4, 8, 9. §4 pipeline della foto (scontorno, rettangolo, colore dominante, applicazione 3D, applicazione piatta, degradazione, CORS) → Task 1, 2, 3, 4, 8. §5 prestazioni (rendering su richiesta, dPR≤2, luci statiche, lazy, cache texture) → Task 8, 9. §6 casi limite (WebGL assente, CORS, scontorno fallito, capo senza foto, outfit vuoto, accessori) → Task 3, 7, 9. §7 verifica → Task 11. Tabella dei costi in §3 → Task 9 e 10. §9 (proxy Gemini) è dichiarato fuori scope e **non ha compiti qui**: è il progetto successivo.

**Coerenza dei tipi.** `GarmentTexture` è definita nel Task 3 (`textureUrl`, `colorHex`, `kind`, `reason`) e consumata con quei nomi nei Task 4, 8, 9. `bodyProfiles` (Task 5) restituisce `torso`, `leg`, `arm`, `head`, `legOffsetX`, `armOffsetX`, `skinHex`, `hairHex`, `hairStyle`, `widthFactor`: il Task 6 legge `body[span.source]` e `body.legOffsetX`, il Task 8 li usa tutti. `garmentProfile` (Task 6) restituisce `{ profile, offsetX, doubled }`, letti con quei nomi nel Task 8. I `kind` di `garmentLayers()` (`top`, `dress`, `bottom`, `outerwear`, `shoes`) corrispondono alle chiavi di `GARMENT_SHAPES`, `GARMENT_ANCHORS`, `SPANS` e a `GARMENT_KINDS`.

**Conteggio dei test.** 58 preesistenti + 11 (Task 1–2) + 7 (Task 5) + 9 (Task 6) = **85**.

**Un rischio dichiarato.** Il Task 8 è l'unico senza test automatici: three.js non gira in ambiente `node`. È deliberato — la logica di geometria è stata spinta fuori, nei moduli puri dei Task 5 e 6, proprio perché fosse testabile. Quel che resta in `Avatar3D.jsx` è cablaggio, e si giudica a schermo nel Task 11. Se durante l'implementazione ci si accorge che in `Avatar3D.jsx` sta ricomparendo della matematica, va spostata in `avatarMesh.js` o `garmentMesh.js` con i suoi test.
